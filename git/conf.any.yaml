- link:
    ~/.gitconfig:
      relink: true
      force: true
      path: .gitconfig

- shell:
    - [
        Create .gitconfig.local for machine specific setting,
        touch ~/.gitconfig.local
      ]
